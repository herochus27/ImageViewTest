# Create React App - Redux - Bootstrap

Available Scripts
In the project directory, you should run:

## npm install
Install all the depedencies.

## npm start
Runs the app in the development mode.
Open http://localhost:3000 to view it in the browser.
